## Containers
Containers are the layers of the images


- Layers of images -> Compliles to the Docker Image
- when we run the docker image, it will create an environment and then tries to run the application

Docker Image:
- Package or an Artifact 
- Move or Share the Artificat

Container:
- when we run the Docker image
- it will create a container, install is installed and then run the application 

